<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail (name, order_id, dish_id, setmeal_id, dish_flavor, number, amount, image)
        values
        <foreach collection="orderDetailList" item="item" separator=",">
            (#{item.name}, #{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.dishFlavor},
            #{item.number}, #{item.amount}, #{item.image})
        </foreach>
    </insert>

    <select id="getSalesTop10" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name, sum(od.number) as number
        from order_detail od
        left join orders o on od.order_id = o.id
        <where>
            <if test="begin != null">
                and o.order_time <![CDATA[>=]]> #{begin}
            </if>
            <if test="end != null">
                and o.order_time <![CDATA[<=]]> #{end}
            </if>
            <if test="status != null">
                and o.status = #{status}
            </if>
        </where>
        group by od.name
        order by number desc
        limit 10
    </select>
</mapper>
